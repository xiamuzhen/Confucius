### Git初始化配置

```gitexclude
git config --global user.name "xiamuzhen"
git config --global user.email "xiamuzhen"

git config --list
git config --list --show-origin

ssh-keygen -r rsa -C "xiamuzhen@163.com"

ssh -T git@github.com
```
